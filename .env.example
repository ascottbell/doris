# =============================================================================
# Doris — Minimal Environment Configuration
# =============================================================================
# Copy this file to .env and fill in the values.
#
# Only ANTHROPIC_API_KEY is strictly required. Everything else has safe defaults.
#
# For the FULL reference of all available settings (integrations, channels,
# security tunables, etc.), see config.py — every setting maps to an env var.
# =============================================================================


# -----------------------------------------------------------------------------
# REQUIRED
# -----------------------------------------------------------------------------

# Claude API key (https://console.anthropic.com/)
ANTHROPIC_API_KEY=


# -----------------------------------------------------------------------------
# API AUTHENTICATION (required for production)
# -----------------------------------------------------------------------------

# Bearer token for the Doris REST API.
# Generate with: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
# DORIS_API_TOKEN=

# Set to true ONLY for local development (bypasses auth checks).
# DORIS_DEV_MODE=false


# -----------------------------------------------------------------------------
# LOCATION (used by weather and calendar scouts)
# -----------------------------------------------------------------------------

# Home coordinates (default: NYC)
DORIS_HOME_LAT=40.7128
DORIS_HOME_LON=-74.0060


# -----------------------------------------------------------------------------
# OPTIONAL — uncomment what you need
# -----------------------------------------------------------------------------

# Your email address (used by email scout)
# DORIS_USER_EMAIL=

# Channel adapter: telegram, bluebubbles, discord, webhook, or empty for API-only
# CHANNEL=

# See config.py for all available settings.
